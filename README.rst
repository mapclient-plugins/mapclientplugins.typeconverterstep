Type Converter
==============

A MAP Client plugin to convert types of providing and using ports from one type to another.
This plugin allows for matching of types that do not exactly match.
The type must be set before any connections are made to the step.

